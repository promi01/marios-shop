---
phase: 01-vertical-mvp-browse-cart-copy-deploy
plan: 07
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - data/inventory.json
autonomous: false
requirements:
  - INV-05
  - FOUND-05
  - DEP-01

must_haves:
  truths:
    - "`data/inventory.json` contains 5–6 realistic placeholder products spanning ≥3 different brands (Tom Ford, Loewe, Creed, MFK, Parfums de Marly, Nishane)."
    - "Inventory exercises the full UI surface: ≥1 `sealed` variant, ≥1 `opened` variant, ≥1 `decant` variant across all products combined."
    - "Exactly one variant has `stock = 0` (exercises the `Εξαντλήθηκε` stub from Plan 03)."
    - "At least one `opened` variant has a `fill_pct` field (data shape exercised — surface deferred to Phase 2)."
    - "Every product has a populated `image_fallback_url` that loads successfully via `next/image` with `unoptimized: true`."
    - "`npm run build` exits 0 against the full inventory and `out/` contains static HTML for every product id."
    - "The Vercel project is linked and the production deploy URL is reachable; the homepage shows all 5–6 products."
    - "The Loewe seed product uses the slug `loewe-bittersweet-oud` AND the display name `Bittersweet Oud` — consistent with the UI-SPEC §Copy-to-Messenger example and Plan 06's `formatOrderText` test fixture (`Loewe — Bittersweet Oud`)."
  artifacts:
    - path: "data/inventory.json"
      provides: "Full Phase 1 seed inventory (5–6 products)"
      contains: "Tom Ford"
  key_links:
    - from: "data/inventory.json"
      to: "lib/inventory.ts (typed import)"
      via: "typed JSON import surfaces all products through getProductById"
      pattern: "products"
---

## Phase Goal

**As a** Facebook visitor opening the production URL, **I want to** land on a real-looking catalog with 5–6 fragrances from recognizable brands and a working end-to-end flow on a public HTTPS URL, **so that** Phase 1 ships as a credible demo of the entire vertical (browse → cart → copy → paste).

<objective>
Replace the Walking Skeleton's 1-product `data/inventory.json` with the full Phase 1 seed (5–6 realistic placeholder products meeting CONTEXT D-01/D-02 requirements). Then ship the production Vercel deploy and confirm the live URL renders end-to-end.

Purpose: Delivers INV-05 (sample inventory with ≥3 real products), FOUND-05 (deployable on Vercel as static site with preview + production URL), DEP-01 (Vercel project linked with auto-deploys from main).

Output: A live, public Marios Shop URL showing the full Phase 1 catalog and supporting the complete cart-copy flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-CONTEXT.md
@.planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-UI-SPEC.md
@lib/types.ts
@lib/inventory.ts
@data/inventory.json

<interfaces>
From `lib/types.ts`:
```typescript
export interface Product { id: string; brand: string; name: string; line?: string; image?: string | null; image_fallback_url: string; notes?: string; description_gr?: string; variants: Variant[]; }
export interface Variant { id: string; type: 'sealed' | 'opened' | 'decant'; size_ml: number; price: number; stock: number; fill_pct?: number; }
```

CONTEXT D-01 anchors the brand list: Tom Ford, Loewe, Creed, Maison Francis Kurkdjian, Parfums de Marly, Nishane.
CONTEXT D-02 anchors the diversity rules: ≥3 brands, mix of types, ≥1 stock=0, ≥1 opened with fill_pct.
CONTEXT D-03 anchors the image strategy: external `image_fallback_url` only; `image` is `null`.

**Canonical Loewe naming** (consistency contract with UI-SPEC §Copy-to-Messenger Format example and Plan 06's test fixture `lib/copy-format.test.ts`): the slug is `loewe-bittersweet-oud` and the display name is `Bittersweet Oud`. The UI-SPEC §Copy-to-Messenger example renders `Loewe — Bittersweet Oud`, and the Plan 06 test asserts the same string. The seed product MUST use these values exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the full Phase 1 seed inventory</name>
  <read_first>
    - .planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-CONTEXT.md D-01 (brand list), D-02 (diversity rules), D-03 (image strategy)
    - .planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-UI-SPEC.md §Copy-to-Messenger Format (the example uses `Loewe — Bittersweet Oud` — the seed MUST match)
    - .planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-06-PLAN.md Task 1 (test fixture uses `brand: 'Loewe', name: 'Bittersweet Oud'`)
    - lib/types.ts (Product, Variant shape)
    - data/inventory.json (Plan 01 minimal seed — will be replaced)
  </read_first>
  <files>data/inventory.json</files>
  <action>
    Replace `data/inventory.json` with a 5-product seed covering all UI surfaces. Use the Write tool with the content below verbatim — every field is intentional (id slugs, types mix, stock numbers, fill_pct on the opened variant, the one stock=0 variant on Creed Aventus 100ml sealed).

    Image URLs use brand-site or fragrantica thumbnail style URLs. Per CONTEXT D-03 these are the only image source in Phase 1; `next.config.ts` has `images.unoptimized: true` so any HTTPS URL works without host allowlisting. If a specific URL 404s during build, swap it with another HTTPS image of the same product — the build only fails if the URL is malformed, not if the resource is missing (`unoptimized: true` defers fetch to runtime).

    **Loewe naming consistency:** the slug `loewe-bittersweet-oud` and name `Bittersweet Oud` are FROZEN by the UI-SPEC §Copy-to-Messenger example and Plan 06's test fixture. Do NOT rename the product (e.g. to `Esencia Loewe Pour Homme`). The `description_gr` and `notes` describe a Loewe oud fragrance; if you want to swap in a more accurate real-world Loewe oud (e.g. `001 Man`, `Solo Loewe Cedro`), keep the slug and name as `bittersweet-oud` / `Bittersweet Oud` so the test fixtures and UI-SPEC remain authoritative. Owner replaces with real data later.

    ```json
    [
      {
        "id": "tom-ford-tobacco-vanille",
        "brand": "Tom Ford",
        "name": "Tobacco Vanille",
        "line": "Private Blend",
        "image": null,
        "image_fallback_url": "https://fimgs.net/mdimg/perfume/375x500.1825.jpg",
        "notes": "καπνός, βανίλια, καρυκεύματα, κακάο, στεγνά φρούτα",
        "description_gr": "Ζεστό oriental προφίλ με καπνό και βανίλια — εμβληματικό άρωμα της Private Blend συλλογής.\nΜοιάζει με χειμωνιάτικη βραδιά κοντά στο τζάκι.",
        "variants": [
          { "id": "tvf-50-sealed", "type": "sealed", "size_ml": 50, "price": 180, "stock": 3 },
          { "id": "tvf-10-decant", "type": "decant", "size_ml": 10, "price": 38, "stock": 6 }
        ]
      },
      {
        "id": "loewe-bittersweet-oud",
        "brand": "Loewe",
        "name": "Bittersweet Oud",
        "line": null,
        "image": null,
        "image_fallback_url": "https://fimgs.net/mdimg/perfume/375x500.7129.jpg",
        "notes": "ξύλα, oud, μπαχαρικά, κέδρος",
        "description_gr": "Ξύλινο-oriental προφίλ με oud, μπαχαρικά και ζεστά ξύλα. Διακριτικό αλλά με χαρακτήρα.",
        "variants": [
          { "id": "bso-100-opened", "type": "opened", "size_ml": 100, "price": 95, "stock": 1, "fill_pct": 85 },
          { "id": "bso-5-decant", "type": "decant", "size_ml": 5, "price": 18, "stock": 8 }
        ]
      },
      {
        "id": "creed-aventus",
        "brand": "Creed",
        "name": "Aventus",
        "line": null,
        "image": null,
        "image_fallback_url": "https://fimgs.net/mdimg/perfume/375x500.9828.jpg",
        "notes": "ανανάς, κάσσι, σημύδα, μοσχοκάρυδο, βανίλια",
        "description_gr": "Φρουτώδες-καπνιστό άρωμα-σημαία. Επιβλητικό αλλά πολυφορεμένο.",
        "variants": [
          { "id": "av-100-sealed", "type": "sealed", "size_ml": 100, "price": 320, "stock": 0 },
          { "id": "av-10-decant", "type": "decant", "size_ml": 10, "price": 55, "stock": 4 }
        ]
      },
      {
        "id": "mfk-baccarat-rouge-540",
        "brand": "Maison Francis Kurkdjian",
        "name": "Baccarat Rouge 540",
        "line": null,
        "image": null,
        "image_fallback_url": "https://fimgs.net/mdimg/perfume/375x500.33519.jpg",
        "notes": "σαφράν, jasmine, ambergris, cedar",
        "description_gr": "Διάφανο amber-φλοράλ προφίλ με γλυκό σαφράν.\nΑναγνωρίσιμο sillage, sweet and warm.",
        "variants": [
          { "id": "br540-70-sealed", "type": "sealed", "size_ml": 70, "price": 280, "stock": 2 },
          { "id": "br540-5-decant", "type": "decant", "size_ml": 5, "price": 32, "stock": 10 }
        ]
      },
      {
        "id": "nishane-hacivat",
        "brand": "Nishane",
        "name": "Hacivat",
        "line": "Extrait de Parfum",
        "image": null,
        "image_fallback_url": "https://fimgs.net/mdimg/perfume/375x500.42440.jpg",
        "notes": "ανανάς, βάλσαμο περού, πατσουλί, σανταλόξυλο",
        "description_gr": "Φρουτώδες-ξύλινο niche, εναλλακτική του Aventus με δικό του χαρακτήρα.",
        "variants": [
          { "id": "hac-50-sealed", "type": "sealed", "size_ml": 50, "price": 145, "stock": 4 },
          { "id": "hac-10-decant", "type": "decant", "size_ml": 10, "price": 32, "stock": 7 }
        ]
      }
    ]
    ```

    Diversity audit against CONTEXT D-02:
    - **5 products from 5 different brands** (Tom Ford, Loewe, Creed, MFK, Nishane) — ≥3 brands ✓
    - **Types covered:** sealed (Tom Ford 50ml, Creed 100ml, MFK 70ml, Nishane 50ml — 4 sealed), opened (Loewe 100ml — 1 opened with `fill_pct: 85`), decant (every product has at least one decant — 5 decants) — all three types present ✓
    - **stock=0:** Creed Aventus 100ml sealed (exercises the `Εξαντλήθηκε` stub) ✓
    - **opened with fill_pct:** Loewe `bso-100-opened` has `fill_pct: 85` ✓
    - **Loewe naming canonical:** slug `loewe-bittersweet-oud`, name `Bittersweet Oud` — matches UI-SPEC §Copy-to-Messenger example and Plan 06 test fixture ✓
    - Plan 02's badge audit will show emerald sealed, amber opened, blue decant badges across the grid.
    - Plan 04's sticky badge will count quantities correctly across multiple add clicks on different products.
    - Plan 06's copy text will produce a multi-item ordered list for the smoke test.

    Notes on URLs: Fragrantica thumbnail URLs are the most stable public CDN for fragrance images and load directly via `next/image unoptimized`. If during execution any URL returns 404 in the browser, swap with an equivalent public URL (the owner will replace these with his own photos later per CONTEXT §Deferred Ideas — Local product photos).

    The notes are in Greek where natural (κανπός / βανίλια etc.) — UI-SPEC §Copywriting Contract does not mandate Greek for the comma-separated `notes` string but UI-06 ("All user-facing strings in Greek") implies Greek is preferred. Mixed-language notes are acceptable for proper-noun ingredients like `oud`, `ambergris` — keep them as-is.

    Build verification: `npm run build`. Expected output:
    - Exit 0.
    - `out/index.html` exists; running `grep -c "Tom Ford" out/index.html` returns ≥ 1.
    - `out/product/tom-ford-tobacco-vanille.html`, `out/product/loewe-bittersweet-oud.html`, `out/product/creed-aventus.html`, `out/product/mfk-baccarat-rouge-540.html`, `out/product/nishane-hacivat.html` all exist.
    - `grep -c "Εξαντλήθηκε" out/product/creed-aventus.html` returns ≥ 1 (the stock=0 variant renders the disabled stub).
    - `grep -c "Bittersweet Oud" out/product/loewe-bittersweet-oud.html` returns ≥ 1 (Loewe canonical name).
    - `npm test` still passes (Plan 06's format tests use synthetic data, not the seed).
  </action>
  <verify>
    <automated>npm test && npm run build && test -f out/product/tom-ford-tobacco-vanille.html && test -f out/product/loewe-bittersweet-oud.html && test -f out/product/creed-aventus.html && test -f out/product/mfk-baccarat-rouge-540.html && test -f out/product/nishane-hacivat.html</automated>
    Additional grep gates:
    - `grep -c "Tom Ford" data/inventory.json` returns ≥ 1
    - `grep -c "Loewe" data/inventory.json` returns ≥ 1
    - `grep -c "Bittersweet Oud" data/inventory.json` returns ≥ 1  (Loewe canonical name — matches UI-SPEC + Plan 06 test fixture)
    - `grep -c "loewe-bittersweet-oud" data/inventory.json` returns ≥ 1  (Loewe canonical slug)
    - `grep -c "Esencia Loewe" data/inventory.json` returns 0  (incorrect name removed)
    - `grep -c "Creed" data/inventory.json` returns ≥ 1
    - `grep -c "Maison Francis Kurkdjian" data/inventory.json` returns ≥ 1
    - `grep -c "Nishane" data/inventory.json` returns ≥ 1
    - `grep -c "\"stock\": 0" data/inventory.json` returns ≥ 1
    - `grep -c "\"fill_pct\":" data/inventory.json` returns ≥ 1
    - `grep -c "\"sealed\"" data/inventory.json` returns ≥ 1
    - `grep -c "\"opened\"" data/inventory.json` returns ≥ 1
    - `grep -c "\"decant\"" data/inventory.json` returns ≥ 1
    - `grep -c "Εξαντλήθηκε" out/product/creed-aventus.html` returns ≥ 1
    - `grep -c "Bittersweet Oud" out/product/loewe-bittersweet-oud.html` returns ≥ 1
    - `grep -c "Tom Ford" out/index.html` returns ≥ 1
    - `grep -c "Loewe" out/index.html` returns ≥ 1
    - `grep -c "Creed" out/index.html` returns ≥ 1
    - `grep -c "Nishane" out/index.html` returns ≥ 1
  </verify>
  <acceptance_criteria>
    - `data/inventory.json` contains exactly 5 product objects from 5 distinct brands.
    - All three variant types (`sealed`, `opened`, `decant`) appear at least once across the array.
    - Exactly one variant has `stock: 0` (Creed Aventus 100ml sealed).
    - At least one `opened` variant has a `fill_pct` field (Loewe — 85).
    - The Loewe product uses slug `loewe-bittersweet-oud` AND display name `Bittersweet Oud` (matches UI-SPEC §Copy-to-Messenger example and Plan 06 test fixture). The seed does NOT use the name `Esencia Loewe Pour Homme`.
    - Every product has a populated `image_fallback_url` HTTPS string.
    - Every product `id` slug is unique and URL-safe (lowercase, hyphens only).
    - Every variant `id` is unique within its product (manual review — also INV-04's build-time validator lands in Phase 2; this is a one-shot manual audit for now).
    - `npm test` passes; `npm run build` exits 0; all 5 product HTML files exist in `out/`.
    - Creed's HTML contains `Εξαντλήθηκε` (proves the stock=0 path is exercised by the seed).
    - Loewe's HTML contains `Bittersweet Oud` (Loewe canonical naming preserved).
  </acceptance_criteria>
  <done>Inventory is real and complete; the build produces the full Phase 1 site locally with the canonical Loewe naming.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Production Vercel deploy (human-only auth + dashboard linking)</name>
  <read_first>
    - .planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-CONTEXT.md (DEP-related decisions)
    - .planning/ROADMAP.md §Phase 1 Success Criteria (visitor can open the deployed Vercel production URL)
    - vercel.json (Plan 01 stub)
  </read_first>
  <what-built>
    Claude has authored the full inventory and verified the local build (`npm run build` exits 0; `out/` contains static HTML for all 5 products). The codebase is ready for production deployment.
  </what-built>
  <how-to-verify>
    The Vercel link step requires you to authenticate in the Vercel CLI/dashboard — Claude cannot complete this part alone. Pick one of the two paths below:

    **Path A — Vercel CLI (faster, if Vercel CLI is installed):**
    1. From the repo root, run:
       ```
       npx --yes vercel@latest login
       ```
       Authenticate via the browser link Vercel opens. This is the step Claude literally cannot do (one-time browser-based auth).
    2. Once logged in, run:
       ```
       npx --yes vercel@latest link
       ```
       Accept defaults to create a new Vercel project named `marios-shop` (or whatever you prefer). This writes `.vercel/project.json` linking the repo to a Vercel project.
    3. Deploy a preview to verify everything builds on Vercel's infrastructure:
       ```
       npx --yes vercel@latest
       ```
       Confirm the build succeeds and copy the preview URL Vercel prints (something like `https://marios-shop-{hash}-{username}.vercel.app`). Open it on a mobile browser (or DevTools mobile emulation) and confirm:
       - Hero "Marios Shop" wordmark visible.
       - All 5 product cards render with images, brand, name, price, badges.
       - Tap a card → product detail loads.
       - Tap "Προσθήκη" → toast appears, sticky cart badge increments to 1.
       - Tap sticky cart → drawer opens with the item.
       - Tap "📋 Αντιγραφή για Messenger" → success toast.
       - Refresh → cart still has the item.
    4. Promote to production:
       ```
       npx --yes vercel@latest --prod
       ```
       Copy the production URL.
    5. (DEP-01) Auto-deploys from `main`: in the Vercel dashboard, navigate to **Project → Settings → Git** and connect the GitHub/GitLab/Bitbucket repository. Set the production branch to `main`. From now on, every push to `main` auto-deploys to the production URL. This is the part of DEP-01 the CLI can't fully automate without the Git provider also being authenticated — the dashboard click-through is the safest path.

    **Path B — Vercel dashboard import (no CLI needed):**
    1. Push the project to a Git host (GitHub recommended). Claude can run `git init && git add . && git commit -m "phase 1 vertical mvp"` but cannot create a remote for you — create an empty GitHub repo (`marios-shop`) yourself, then back in the terminal:
       ```
       git remote add origin https://github.com/{you}/marios-shop.git
       git branch -M main
       git push -u origin main
       ```
       (Claude can prepare the commit; you handle the GitHub repo creation and the `git push`.)
    2. Open https://vercel.com/new and **Import** the `marios-shop` repo. Accept the auto-detected Next.js settings (Vercel reads `next.config.ts`'s `output: 'export'` and serves `out/` as a static site).
    3. Click **Deploy**. Verify the production URL.

    Once Path A or B completes, paste the production URL into the resume signal so it can be recorded in the SUMMARY.
  </how-to-verify>
  <resume-signal>Paste the production Vercel URL (e.g. `https://marios-shop.vercel.app`) OR describe any deploy issue (build error log, missing dependency, etc.).</resume-signal>
  <done>The production Vercel URL is live, the homepage shows 5 products, end-to-end smoke test passes on the deployed URL, and (Path A or B) auto-deploys from `main` are configured.</done>
</task>

</tasks>

<verification>
- Local: `npm test` passes; `npm run build` exits 0; `out/` contains 5 product HTML files; Creed page has the `Εξαντλήθηκε` stub; Loewe page contains `Bittersweet Oud` (canonical naming).
- Remote: production Vercel URL is reachable; the full smoke test (browse → add → drawer → copy) works on the live site.
- DEP-01: `main` branch pushes auto-deploy to production.
</verification>

<success_criteria>
- INV-05: 5 realistic placeholder products covering all variant types, stock states, and fill_pct on the data side.
- FOUND-05: Project is deployable on Vercel as a static site with preview and production URLs.
- DEP-01: Vercel project is linked with auto-deploys from `main`.
- ROADMAP Phase 1 Success Criterion 1 (visitor can open production URL on mobile and see hero + ≥3 products) is met.
- Loewe canonical naming (`loewe-bittersweet-oud` / `Bittersweet Oud`) is consistent across UI-SPEC, Plan 06 test fixture, and the seed JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/01-vertical-mvp-browse-cart-copy-deploy/01-07-SUMMARY.md` documenting:
- The 5 seed product ids (so any future SUMMARY indexer can audit them).
- The production Vercel URL (from the checkpoint resume signal).
- The chosen deploy path (CLI vs dashboard).
- Confirmation of the live-URL smoke test results.
- Confirmation that the Loewe product's slug/name (`loewe-bittersweet-oud` / `Bittersweet Oud`) matches the UI-SPEC and Plan 06 test fixture.
- Note that owner will replace these placeholder products with his actual collection by editing `data/inventory.json` and pushing to `main`.
</output>

## Objective — Decision Coverage

This plan addresses the following locked decisions from `01-CONTEXT.md`:

- **D-01:** Phase 1 ships with 5–6 placeholder products (realistic fake fragrances — Tom Ford, Loewe, Creed, MFK, Nishane); owner replaces with actual collection by editing `data/inventory.json`
- **D-02:** seed data exercises full UI surface — ≥3 brands, mix of sealed/opened/decant variants, ≥1 variant with `stock = 0`, ≥1 `opened` variant with `fill_pct`
- **D-03:** product images use `image_fallback_url` only (public URLs from brand sites / fragrantica thumbnails); no local `/public/products/*.jpg` files in Phase 1
